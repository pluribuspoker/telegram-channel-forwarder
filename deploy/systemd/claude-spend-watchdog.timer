[Unit]
Description=Run the Claude spend watchdog hourly

[Timer]
# :07 past the hour — off the :00/:05 tracker cadence so the journal read
# isn't competing with a grading run.
OnCalendar=*:07
Persistent=true

[Install]
WantedBy=timers.target
